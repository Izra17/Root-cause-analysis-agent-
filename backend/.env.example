# Copy this file to .env
# Keep the real key ONLY in backend/.env. Never put it in frontend code.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Current model can be changed without touching frontend code.
ANTHROPIC_MODEL=claude-sonnet-5

PORT=4000
FRONTEND_ORIGIN=http://localhost:5173
