import React from "react";
import ReactDOM from "react-dom/client";
import RCAAgent from "./RCAAgent.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RCAAgent />
  </React.StrictMode>
);
